# Signature-Authenticator
A machine learning–based web application that allows users to register their genuine signatures and verify new signatures in real time. The system uses image processing and SVM classification to authenticate whether a signature is valid or forged.  Users first upload multiple genuine signature samples to train a personalized model. 
